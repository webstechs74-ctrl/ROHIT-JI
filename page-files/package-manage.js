// Separate UI entry for: package-manage
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["package-manage"] = function render_package_manage(){
  return page_packageManage();
};
