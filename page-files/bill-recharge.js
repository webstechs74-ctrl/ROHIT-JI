// Separate UI entry for: bill-recharge
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["bill-recharge"] = function render_bill_recharge(){
  return page_billRecharge();
};
