// Separate UI entry for: all-service
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["all-service"] = function render_all_service(){
  return page_allService();
};
