// Separate UI entry for: sr-commission-set
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["sr-commission-set"] = function render_sr_commission_set(){
  return page_srCommissionSet();
};
