// Separate UI entry for: api-partner
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["api-partner"] = function render_api_partner(){
  return page_apiPartner();
};
