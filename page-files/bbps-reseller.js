// Separate UI entry for: bbps-reseller
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["bbps-reseller"] = function render_bbps_reseller(){
  return page_bbpsReseller();
};
