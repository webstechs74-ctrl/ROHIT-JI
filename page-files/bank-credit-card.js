// Separate UI entry for: bank-credit-card
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["bank-credit-card"] = function render_bank_credit_card(){
  return page_bankCreditCard();
};
