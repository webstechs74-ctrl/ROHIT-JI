// Separate UI entry for: sr-review-website
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["sr-review-website"] = function render_sr_review_website(){
  return page_srReviewWebsite();
};
