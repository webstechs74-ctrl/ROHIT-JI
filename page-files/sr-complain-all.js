// Separate UI entry for: sr-complain-all
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["sr-complain-all"] = function render_sr_complain_all(){
  return page_srComplainAll();
};
