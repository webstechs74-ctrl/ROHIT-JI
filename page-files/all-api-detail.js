// Separate UI entry for: all-api-detail
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["all-api-detail"] = function render_all_api_detail(){
  return page_allApiDetail();
};
