// Separate UI entry for: add-service
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["add-service"] = function render_add_service(){
  return page_addService();
};
