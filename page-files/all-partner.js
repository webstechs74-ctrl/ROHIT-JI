// Separate UI entry for: all-partner
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["all-partner"] = function render_all_partner(){
  return page_allPartner();
};
