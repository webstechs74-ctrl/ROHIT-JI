// Separate UI entry for: fm-wallet-debit
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["fm-wallet-debit"] = function render_fm_wallet_debit(){
  return page_fmWalletDebit();
};
