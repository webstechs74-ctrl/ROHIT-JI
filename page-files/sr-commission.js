// Separate UI entry for: sr-commission
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["sr-commission"] = function render_sr_commission(){
  return page_srCommission();
};
