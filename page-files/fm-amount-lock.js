// Separate UI entry for: fm-amount-lock
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["fm-amount-lock"] = function render_fm_amount_lock(){
  return page_fmAmountLock();
};
