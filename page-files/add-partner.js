// Separate UI entry for: add-partner
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["add-partner"] = function render_add_partner(){
  return page_addPartner();
};
