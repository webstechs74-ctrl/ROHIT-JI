// Separate UI entry for: fm-amount-release
// Replace the function body below when only this page needs a custom UI.
PAGE_RENDERERS["fm-amount-release"] = function render_fm_amount_release(){
  return page_fmAmountRelease();
};
